"""Pipeline stages for neural_pass (stubs).

Order used by `pipeline.run_pass`:
  aov_ingest → conditioning → generate → edit_loop → flywheel

`train_style` is a sibling stage invoked via its own CLI.
"""
